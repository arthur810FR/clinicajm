spring:
  datasource:
    url: jdbc:mysql://localhost:3306/clinicajm?useSSL=false&createDatabaseIfNotExist=true
    username: root
    password: "Caramelo"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        show_sql: true
        hbm2ddl:
          auto: update
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML5
    encoding: UTF-8
    cache: false
